{
  "schema_version": 1,
  "id": "cache-analyzer",
  "name": "CacheAnalyzer",
  "version": "0.1.0",
  "description": "Test cache-key discrepancies, cache poisoning, and web cache deception with cache-busted controls and confirmation.",
  "enabled": true,
  "entrypoint": "index.js",
  "entrypoint_sha256": "5df5d57696ad6af82b72c936509557e413aa590885875ac691b6727eda73ce4d",
  "capabilities": ["http.semantic"],
  "limits": { "timeout_ms": 600000, "max_operations": 2000, "max_concurrency": 1, "memory_mb": 64 },
  "actions": [{
    "name": "scan",
    "description": "Probe one saved request for poisoning and deception behaviors, requiring clean-control confirmation.",
    "required_capabilities": ["http.semantic"],
    "input_schema": { "type": "object", "additionalProperties": false, "required": ["marker", "allow_cache_side_effects"], "properties": { "marker": { "type": "string", "pattern": "^[A-Za-z0-9]{8,40}$", "description": "Unique per-run marker generated by the caller." }, "allow_cache_side_effects": { "const": true, "description": "Explicit acknowledgement that the test may create isolated cache entries." }, "modes": { "type": "array", "uniqueItems": true, "items": { "enum": ["poisoning", "deception"] } }, "max_poison_variants": { "type": "integer", "minimum": 1, "maximum": 8, "default": 8 } } }
  }]
}
