{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://huntproxy.local/schemas/plugin-manifest-v1.json",
  "title": "HuntProxy plugin manifest v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id", "name", "version", "description", "enabled", "entrypoint", "entrypoint_sha256", "capabilities", "limits", "actions"],
  "properties": {
    "schema_version": { "const": 1 },
    "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{1,62}$" },
    "name": { "type": "string", "minLength": 1, "maxLength": 80, "pattern": ".*\\S.*" },
    "version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "description": { "type": "string", "minLength": 1, "maxLength": 300 },
    "enabled": { "type": "boolean" },
    "entrypoint": { "type": "string", "pattern": "^(?:[A-Za-z0-9_-][A-Za-z0-9._-]*/)*[A-Za-z0-9_-][A-Za-z0-9._-]*\\.js$" },
    "entrypoint_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "resources": {
      "type": "object",
      "propertyNames": { "type": "string", "minLength": 1, "maxLength": 64 },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256"],
        "properties": {
          "path": { "type": "string", "minLength": 1, "pattern": "^(?:[A-Za-z0-9_-][A-Za-z0-9._-]*/)*[A-Za-z0-9_-][A-Za-z0-9._-]*$" },
          "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
        }
      }
    },
    "capabilities": {
      "type": "array",
      "uniqueItems": true,
      "items": { "enum": ["http.semantic", "http.raw", "http.race", "identity.use", "page.discover", "aws.api_gateway"] }
    },
    "limits": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_concurrency", "max_operations", "timeout_ms", "memory_mb"],
      "properties": {
        "max_concurrency": { "type": "integer", "minimum": 1, "maximum": 100 },
        "max_operations": { "type": "integer", "minimum": 1, "maximum": 10000 },
        "timeout_ms": { "type": "integer", "minimum": 1000, "maximum": 900000 },
        "js_stage_timeout_ms": { "type": "integer", "minimum": 250, "maximum": 120000, "description": "Optional bounded budget for each JavaScript plan/analyze stage; first-party plugins use 60000 ms and the legacy host fallback remains 2000 ms." },
        "memory_mb": { "type": "integer", "minimum": 4, "maximum": 128 }
      }
    },
    "actions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "description", "input_schema"],
        "properties": {
          "name": { "type": "string", "pattern": "^[a-z][a-z0-9_]{1,63}$" },
          "description": { "type": "string", "minLength": 1, "maxLength": 300 },
          "input_schema": {
            "type": "object",
            "required": ["type"],
            "properties": { "type": { "const": "object" } },
            "description": "Discovery schema shown to callers. The plugin must still validate safety-sensitive input inside plan()."
          },
          "required_capabilities": {
            "type": "array",
            "uniqueItems": true,
            "items": { "enum": ["http.semantic", "http.raw", "http.race", "identity.use", "page.discover", "aws.api_gateway"] }
          }
        }
      }
    }
  }
}
